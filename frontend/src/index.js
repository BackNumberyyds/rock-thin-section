import React from 'react';
import ReactDOM from 'react-dom';
import Widget from './checkbox-tree';
import SearchForm from './allphoto-search-form';

ReactDOM.render(<SearchForm />, document.getElementById('root'));
