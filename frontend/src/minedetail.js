import React from 'react';
import ReactDOM from "react-dom/client";
import App from './mine-detail/charts'

const rootElement = document.getElementById("root");
ReactDOM.createRoot(rootElement).render(<App />);
